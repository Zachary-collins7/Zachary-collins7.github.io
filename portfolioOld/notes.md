


page.template {
    .nav
    hero
    sections = {
        sectionHero
            animate bg and fixed
        sectionContent
            animate show
        children elements custom
            preformance? binding selected? for get data

    }
}
